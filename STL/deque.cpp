#include<bits/stdc++.h>
using namespace std;

void dequeSolve()
{
    deque<int> dq;
    dq.push_back(10);
    dq.push_front(20);

    dq.back();
    dq.front();

    // rest functions are same as vector

}

int main()
{
    dequeSolve();
}